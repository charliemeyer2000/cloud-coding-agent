/**
 * Shared between the agent (server) and the UI (client) — keep this file
 * dependency-free so importing it doesn't drag server code into the bundle.
 */

/** Placeholder stored in the DB instead of screenshot bytes (see chat route). */
export const SCREENSHOT_OMITTED = "[screenshot omitted]";
